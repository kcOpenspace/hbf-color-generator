import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  cssVariables: true,
  palette: {
    primary: {
      main: '#009FAE',
    },
    secondary: {
      main: '#003959',
    },
  },
});

export default theme